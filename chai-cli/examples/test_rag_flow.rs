use anyhow::Result;
use chai_core::{Config, ai};

#[tokio::main]
async fn main() -> Result<()> {
    dotenvy::dotenv().ok();

    let api_key = std::env::var("OPENROUTER_API_KEY")?;
    let config = Config::from_env()?;

    // Тестовые запросы
    let queries = vec![
        "Кислый чай с облепихой",
        "Успокаивающий чай на ночь",
        "Бодрящий утренний чай",
    ];

    println!("╔════════════════════════════════════════════════════════════════╗");
    println!("║           🧪 ТЕСТ AI ПРОМПТА (РЕАЛЬНЫЙ WORKFLOW)               ║");
    println!("╚════════════════════════════════════════════════════════════════╝\n");

    for query in queries {
        println!("\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");
        println!("📝 Запрос: {}", query);
        println!("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n");

        match ai::chat_completion(query.to_string(), api_key.clone(), &config).await {
            Ok(response) => {
                println!("💬 Общий ответ:");
                println!("┌────────────────────────────────────────────────────────────┐");
                println!("│ {}", response.answer);
                println!("└────────────────────────────────────────────────────────────┘\n");

                println!("🍵 Рекомендованные чаи ({}):\n", response.tea_cards.len());

                for (i, card) in response.tea_cards.iter().enumerate() {
                    println!(
                        "{}. {} {}",
                        i + 1,
                        card.title,
                        if card.in_stock { "✅" } else { "❌" }
                    );
                    println!("   🔗 {}", card.url);
                    println!("   🏷️  {}", card.tags.join(", "));
                    println!("   💡 {}", card.short_description);
                    println!("   📊 Score: {:.2}", card.match_score);
                    if let Some(price) = &card.price {
                        println!("   💰 {}", price);
                    }
                    println!();
                }
            }
            Err(e) => {
                println!("❌ Ошибка: {}", e);
            }
        }

        println!("{}", "═".repeat(64));
    }

    Ok(())
}
